vis {
    title: "Two arrows in sequence with node capacities and split multiplicities"
}

cap {
    2 a b,
    3 c,
}

mul {
    2 a -> b,
    2 b <- a,
    2 b -> c,
    0 c <- b,
}

ces Main {
    a => b => c
}
